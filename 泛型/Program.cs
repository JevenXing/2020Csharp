using System;
using 泛型partial类枚举结构体;
namespace 泛型
{
    /// <summary>
    /// 避免成员膨胀或类型膨胀
    /// 
    /// </summary>
    class Program
    {
        static void Main(string[] args)
        {
            //泛型类.Invoke();
            //泛型接口.Invoke();
            //泛型在算法中的应用.Invoke();
            //泛型委托.invoke();
            //partial类.Invoke();
            枚举类型.invoke();
        }
    }
   
}
